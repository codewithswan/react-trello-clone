import styled from 'styled-components'
import { fontWeights, textSizes } from "../styles/constants";

export const HeadingInput = styled.input`
  font-size: ${textSizes.h2};
  font-weight: ${fontWeights.heading};
  border: none;
`